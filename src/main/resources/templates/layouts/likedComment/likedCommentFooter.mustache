<form>
    <div class="post_footer">
        <input type="hidden" name="postId" value="{{{postId}}}">
        <input type="hidden" name="commentId" value="{{{commentId}}}">
        <button formaction="/comment" formmethod="get">
            <span style="cursor: pointer;" class="material-icons">shortcut</span>
        </button>
        <button formaction="/comment/like/cancel" formmethod="post">
            <span style="cursor: pointer;" class="material-icons">favorite_border</span>{{commentLikeCount}}
        </button>
    </div>
</form>